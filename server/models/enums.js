// RACE ENUMS
let READY = Symbol("ready");
let STARTED = Symbol("started");
let FINISHED = Symbol("finished");
let CANCELED = Symbol("canceled");

// GENDER ENUMS


// SKIN TONE ENUMS

module.exports = {READY, STARTED, FINISHED, CANCELED};

